import type { ApiEndpoint } from '../../../types/api';
import { zuoraEnvironments } from '../../environments';

export const getsystemhealthapifailurerecordsEndpoint: ApiEndpoint = {
  "id": "getsystemhealthapifailurerecords",
  "name": "List API failure records",
  "description": "Returns failure records of API usage that are generated by your Zuora tenant within a specified time range.",
  "method": "GET",
  "path": "/system-health/api-requests/failures",
  "baseUrl": "https://rest.test.zuora.com",
  "environments": zuoraEnvironments,
  "requiresAuth": true,
  "authType": "bearer",
  "bodyFields": [],
  "headers": {
    "Content-Type": "application/json"
  }
};
